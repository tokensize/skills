interface:
  display_name: "Delegate"
  short_description: "Route work to the best local coding agent"
  default_prompt: "Use $delegate to discover my local coding agents and safely delegate this task through TokenSize."
